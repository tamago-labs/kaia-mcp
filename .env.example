# KAIA-MCP Server Configuration

# Private key for transaction signing (optional for read-only operations)
# WARNING: Keep this secure and never commit to version control
PRIVATE_KEY=your_private_key_here

# KAIA RPC URL (defaults to mainnet)
RPC_URL=https://public-en.node.kaia.io

# KAIA-MCP API endpoints
API_BASE_URL=https://kvxdikvk5b.execute-api.ap-southeast-1.amazonaws.com/prod
PRICE_URL=https://kvxdikvk5b.execute-api.ap-southeast-1.amazonaws.com/prod/prices

# Optional: Agent mode (read-only or transaction)
# read-only: Only query data, no transactions
# transaction: Full access including transactions
AGENT_MODE=read-only

# Optional: Custom RPC configuration
# RPC_TIMEOUT=10000
# RPC_RETRIES=3

# Optional: Log level (error, warn, info, debug)
LOG_LEVEL=info

# Optional: Enable debug mode
DEBUG=false
